name: traefik

on:
  push:
    branches: [main]
    paths:
      - traefik/**
      - .github/workflows/traefik.yml
      - .github/workflows/build.yml

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      container: traefik
