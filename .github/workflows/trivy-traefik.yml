name: trivy-traefik

on:
  schedule:
    - cron: '30 5 * * *'
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  scan:
    uses: ./.github/workflows/trivy.yml
    with:
      image: traefik
