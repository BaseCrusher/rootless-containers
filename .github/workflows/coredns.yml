name: coredns

on:
  push:
    branches: [main]
    paths:
      - coredns/**
      - .github/workflows/coredns.yml
      - .github/workflows/build.yml

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      container: coredns
