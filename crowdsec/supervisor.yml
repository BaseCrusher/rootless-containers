hide_labels: true

processes:
  register:
    path: /usr/local/bin/cscli
    arguments: ["machines", "add", "localhost", "--auto", "--force"]
    type: one_shot
  crowdsec:
    path: /usr/local/bin/crowdsec
    type: service
    depends_on:
      register:
        exit: success
